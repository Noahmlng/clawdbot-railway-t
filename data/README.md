# data workspace snapshot

This directory is a local snapshot of the Railway runtime `/data/workspace` folder.

It is not the full `/data` volume. Runtime state such as `/data/.openclaw` and nested Git metadata should stay out of version control.
